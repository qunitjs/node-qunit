#!/usr/bin/env node

var root = require( "path" ).normalize( __dirname + "/.." ),
	testrunner = require( root );

testrunner.run([
    {
        code: root + "/lib/api.js",
        tests: [ root + "/test/api.js",  root + "/test/same.js"]
    }
]);