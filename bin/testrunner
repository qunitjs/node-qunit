#!/usr/bin/env node

var root = require( "path" ).normalize( __dirname + "/.." ),
	testrunner = require( root + "/lib" );

testrunner.run([
    {
        code: root + "/lib/qunit.js",
        tests: [ root + "/test/qunit.js",  root + "/test/same.js"]
    }
/*    ,
    {
        code: root + "/lib/index.js",
        test: root + "/test/testrunner.js"
    }
*/        
]);